import React from 'react';
import ExpenseLineChart from "./ExpenseLineChart";

const Analysis = () => {
    return (
        <div>
            <ExpenseLineChart/>
        </div>
    );
};

export default Analysis;