import React from 'react';
import styles from '../../../styles/challenge/Boards.module.scss'

const Boards = () => {
    return (
        <div>
            
        </div>
    );
};

export default Boards;